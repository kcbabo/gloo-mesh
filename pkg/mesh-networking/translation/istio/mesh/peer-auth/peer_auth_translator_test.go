package peerAuth_test

import (
	. "github.com/onsi/ginkgo"
)

var _ = Describe("PeerAuthTranslator", func() {

	// Todo add tests that:
	// - Ensure that peer auth's are actually created... whatever that apparently means
	// - Anything else?

})
